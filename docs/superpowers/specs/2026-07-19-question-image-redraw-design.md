# 题库图片统一重绘与 OSS 迁移设计

## 1. 目标

将当前本地数据库中正在被题目引用的图片全部重新绘制为原创、无水印、统一黑白风格的技术图表。在不改变题意和正确答案的前提下，允许优化排版、对齐、留白与信息层级；不得把标准答案或能够直接推导出填空答案的新增提示画入图片。

本设计分两个阶段执行：

1. 完成全部图片的 SVG 重绘、PNG 导出和总览验收；不修改数据库。
2. 用户验收后上传 OSS，备份并批量替换数据库中的旧图片 URL。

## 2. 精确范围

以当前运行中的本地 `aisoftoj.question` 表为唯一数据源，过滤 `is_deleted = 0`，检查 `intro`、`options`、`answer`、`analysis` 四个字段中的 Markdown 图片和 HTML 图片引用。第一阶段数据库连接必须使用只读账号或只读事务。

精确盘点结果：

- 有效题目：1163 道。
- 包含图片的题目：32 道。
- 图片 URL 引用次数：33 次。
- 唯一图片 URL：28 个。
- 系统架构设计师：25 张唯一图片。
- 系统分析师：3 张唯一图片。
- 两张图片分别被 4 道和 3 道连续小题复用；只重绘和上传一次。
- 一道题包含两张不同图片，两张都需要独立重绘。

普通文章链接、用户头像、README 图片、指南图片和历史导出目录中尚未导入数据库的图片不在本次范围内。

开始第一阶段时生成不可变的 occurrence-level 快照。快照记录数据库身份（主机、端口、schema，不记录凭据）、提取时间、run ID、题目 ID、字段名、图片节点序号、Markdown/HTML 语法类型、原始 URL、完整原字段 SHA-256 和对应 asset ID。用户验收绑定到该快照；第二阶段不得重新解释或扩大范围。

去重仅按原始 URL 的完整字节串执行，不自动修改大小写、编码、查询参数、末尾斜杠或重定向地址。下载后的最终 URL 与源文件 SHA-256 只作为审计信息。若不同原始 URL 下载到相同内容，可在人工确认后共享绘图源，但数据库替换仍保留各自的 occurrence 映射。

## 3. 方案选择

### 3.1 采用方案：确定性 SVG 重绘

每张图使用可编辑 SVG 表达，统一字体、颜色、线宽、箭头、表格、节点和留白，再从 SVG 导出高分辨率 PNG。

选择原因：

- 中文文字、编号、连线和拓扑关系可以精确控制。
- 可以稳定复现题目中的空白位置和编号占位。
- 统一风格后仍可针对复杂图表单独调整布局。
- SVG 可作为长期可维护源文件，PNG 可兼容现有 Markdown/HTML 图片展示。

### 3.2 不采用的方案

- AI 看图生成：容易产生中文错字、编号漂移、错误连线和答案泄露。
- 原图修补或像素级水印擦除：容易残留痕迹，无法统一视觉风格，也不利于后续维护。

AI 仅可辅助理解原图，不作为最终图中文字和结构的生成来源。

## 4. 内容重建规则

每张图片的内容由三类信息共同确定：

1. 原图用于识别整体结构、节点位置、箭头方向和表格形态。
2. 题目的 `intro` 用于确认题目要求、候选项、编号与空白位置。
3. `answer` 和 `analysis` 仅在运行时用于内部校验“不能画出的内容”，不得复制到 SVG 元数据、manifest、联系表、审核报告或 Git 文件中。校验输出只能记录通过/失败和需要人工判断的题目 ID，不输出答案文本。

规则如下：

- 保留题意所需的标题、实体名称、关系、箭头方向、编号、边界、表头和图例。
- 填空处继续使用 `(1)`、`(2)`、`a`、`b` 等原题占位，不填答案。
- 如果原图本身已经用红字、标注或高亮展示答案，最终图必须恢复为空白占位或原题编号。
- 删除来源网站名称、公众号、二维码、Logo、斜向水印、推广语和与答题无关的装饰。
- 不添加新的提示性颜色、说明文字或视觉强调。
- 当原图文字与题干明显冲突时，记录为人工复核项，不自行猜测。

每个 asset 必须有一份审核记录，明确列出允许显示的文字清单、必须保留的占位符、节点清单、边清单与方向、表格行列结构，以及所有原图冲突的处理结论。最终可见文字超出允许清单即审核失败。

## 5. 统一视觉规范

- 画布：白色背景，内容自适应宽高，四周保留至少 48 px 安全边距。
- 主色：`#111111`。
- 次级文字与辅助线：`#555555`、`#888888`。
- 禁止：彩色填充、渐变、阴影、纹理、Logo、水印和二维码。
- 字体：固定使用 `Noto Sans CJK SC`；渲染环境缺少该字体时直接失败，不静默回退。
- 正文字号：导出后等效 24–30 px；小型表格不得低于 20 px。
- 线宽：主结构 2.5 px，辅助线 1.5–2 px。
- 节点：圆角矩形、圆形、菱形和数据库圆柱采用统一描边风格。
- 箭头：统一箭头尺寸和端点留白，避免箭头穿过文字或节点。
- 表格：统一单线边框、居中表头、内容按语义左对齐或居中。
- 图内不得出现最终答案专用的红色、绿色或高亮状态。
- SVG 必须是自包含的可编辑矢量文件：文字保持为文字，结构保持为矢量；禁止嵌入原始位图、脚本、`foreignObject`、远程资源、隐藏答案文字、不可见图层和跟踪元数据。
- PNG 使用固定版本的渲染器生成，像素尺寸严格等于 SVG `viewBox` 的 2 倍。
- 在答题页等效 360 px 和 960 px 展示宽度下不得出现裁切、文字重叠或箭头遮挡；最小可见文字不得低于 12 CSS px。

## 6. 文件组织与命名

工作中间产物保存在被 Git 忽略的本地目录，审核通过后的 SVG、manifest 与审核记录进入可版本化目录：

```text
assets/question-images-redrawn/<run-id>/
├── working-manifest.json
├── source/
├── png/
└── qa/contact-sheet.png

question-assets/redrawn/<run-id>/
├── manifest.json
├── svg/
│   └── <asset-id>.svg
└── qa/
    ├── review-report.md
    └── approval.json

.local-backups/question-image-redraw/<run-id>/
├── database-backup.json
├── database-backup.sha256
└── rollback-report.md
```

`asset-id` 使用原始 URL 完整字节串的 SHA-256；不得截短到存在碰撞风险的长度。manifest 以 asset 为顶层记录，一个 asset 下保存全部 occurrence 数组，确保复用图片只有一份 SVG/PNG。每个 occurrence 保留原始 URL 和完整原字段哈希，用于精确替换与并发校验。

当前 `assets/` 已被 `.gitignore` 忽略，因此下载原图、PNG 和联系表不会意外进入 Git。审核通过后的 SVG、manifest、审核报告和审批摘要进入 `question-assets/`，作为长期可维护源。含完整题目字段的数据库备份只能进入权限为 `600` 的 `.local-backups/`，不得进入 Git、OSS 公共目录或审核页面。备份至少保留到迁移后 90 天，并在删除前再次确认无需回滚。

## 7. 重绘流水线

### 7.1 清单生成

- 从数据库重新提取图片引用。
- 按原始 URL 完整字节串去重，不做隐式规范化。
- 记录每个 URL 对应的题目 ID、题目名称、字段、图片节点序号、语法类型、完整原字段 SHA-256 和 occurrence 顺序。
- 下载只允许 `https` 或显式批准的 `http`；拒绝携带凭据的 URL、非网络协议、loopback、私网、链路本地和云元数据地址，并在每次 DNS 解析和重定向后重新检查。
- 设置连接与读取超时、最大重定向次数、最大下载字节、最大解码像素和最大图片尺寸；同时校验响应 MIME、文件 magic bytes 和安全解码结果。
- 记录最终 URL、源文件字节数、像素尺寸和源文件 SHA-256。
- 下载失败、无法解码或语义无法可靠识别的 asset 必须阻断第一阶段完成，不得跳过或猜测；只有用户记录明确处置后才能继续。

### 7.2 单图重绘

- 检查原图与题干，列出必须保留的文字和关系。
- 明确所有不可出现的答案内容。
- 使用共享 SVG 组件绘制节点、箭头、表格、时序图、数据流图等。
- 对复杂图表允许改变布局，但不能改变语义或连线方向。
- SVG 仅使用自包含文字和矢量元素，不嵌入原图。
- 使用固定版本渲染器与固定字体导出严格 2 倍 viewBox 尺寸的 PNG。

### 7.3 自动检查

- SVG/XML 可解析。
- PNG 能正常解码，尺寸和文件大小合理。
- 四角和主要背景为白色，不存在透明异常。
- 文件中不包含已知来源域名、水印文案、公众号或二维码文本。
- SVG 不包含脚本、`foreignObject`、远程引用、嵌入位图、隐藏文字或不可见图层。
- 图中文字清单与 asset 的允许标签清单一致。
- 标准答案中的敏感词若出现在图片中，必须人工判断其是否为题干本来就要求展示的内容；工具只输出失败项，不输出答案文本。
- 在 360 px 和 960 px 预览宽度下执行裁切、重叠和最小字号检查。

### 7.4 人工验收

生成 28 张图片的联系表和逐图审核报告。审核项包括：

- 结构与原题一致。
- 编号与空白位置正确。
- 无答案泄露。
- 无水印和推广信息。
- 中文无错字。
- 黑白风格一致。
- 缩放到实际答题页面宽度后仍清晰可读。

每个 asset 的 review 状态、SVG SHA-256、PNG SHA-256 和审核人/时间写入 manifest。生成联系表后计算 manifest SHA-256；用户必须明确批准该 manifest digest。`approval.json` 只记录 run ID、manifest digest、28 个 PNG digest、批准时间和批准结论，不记录题目答案。任何 SVG、PNG、映射或审核状态变化都会使批准失效并返回第一阶段重新验收。

用户验收前不上传 OSS、不更新数据库。

## 8. OSS 上传设计

用户验收后，将 PNG 上传到内容寻址路径：

```text
aisoftoj/questions/redrawn/<png-sha256>.png
```

目标 Bucket 为 `aisoftoj`，地域为华北 2（北京），Endpoint 为 `oss-cn-beijing.aliyuncs.com`。题库只能保存稳定的 HTTPS 公共/CDN URL，不能保存临时签名 URL。实现不得隐式改变 Bucket 或 Object ACL；如果新对象无法通过既有稳定域名匿名 GET，第二阶段阻断并请求用户配置访问策略。

凭据只能来自环境变量或本机凭据配置，不得进入日志、manifest、备份、Git、命令输出或审核页面。SVG 源文件以版本化工作区目录为主；如需 OSS 归档，必须放入独立私有前缀并另行批准，题库引用始终使用 PNG。

上传要求：

- `Content-Type` 正确设置为 `image/png`。
- 上传前重新计算 manifest 与全部 PNG SHA-256，必须与 `approval.json` 完全一致。
- 使用条件式不覆盖上传；对象已存在时，只有远端字节长度和 SHA-256 与本地一致才视为成功，否则停止。
- 上传后通过稳定 URL GET 对象，检查 TLS 主机、HTTP 状态、`Content-Type`、字节长度和 SHA-256。
- 新 URL 写回 `manifest.json`，禁止依赖上传顺序进行映射。
- 任一图片上传失败时停止数据库迁移，manifest 记录已完成对象供幂等重试，不产生半更新题库。
- 部分上传对象在迁移成功并超过回滚观察期后，才能根据 manifest 生成清理候选；不自动删除对象。

## 9. 数据库迁移与回滚

上传全部成功并完成用户验收后：

1. 在任何更新前生成权威备份，包含 run ID、数据库身份、受影响题目的完整四字段、原始字段哈希，并生成独立 SHA-256 校验文件。备份文件权限必须为 `600`。
2. 在隔离的临时 schema 中导入备份并比较字段哈希，证明备份可恢复。
3. 第二阶段预检重新解析数据库，要求精确满足 `1163` 条有效题目、`32` 条受影响题目、`33` 个目标图片 occurrence、`28` 个 asset，并要求 occurrence 的题目 ID、字段、节点序号、原始 URL 和完整字段哈希与批准 manifest 一致；否则不上传或不迁移。
4. 开启事务，对 32 条目标题目执行 `SELECT ... FOR UPDATE`，再次解析并核对所有字段哈希，防止预检后并发修改。
5. 使用 Markdown/HTML 语法感知方式，只替换 manifest 中列出的图片节点 URL；不得对整段文本执行无上下文的字符串替换，也不得修改普通链接或非目标内容。
6. 更新后在同一事务内断言：33 个旧图片节点变为 0、33 个预期新图片节点存在、正好影响 32 条题目、所有非目标内容字节级不变、每个迁移后字段哈希与 manifest 计算值一致。
7. 任一数量或哈希不匹配立即回滚；全部通过才提交。
8. “旧 URL 清零”仅指 `is_deleted = 0` 的题目四字段中、manifest 指定的图片节点，不包含软删除行或普通超链接。
9. 提交后通过题目 API 和前端答题页抽查，并记录迁移后字段哈希。

回滚也必须在事务中执行。恢复前先锁定目标行，并要求当前字段哈希与 manifest 中的迁移后哈希一致；若迁移后又发生编辑，则拒绝普通回滚，只有用户明确批准 force 模式后才能覆盖。回滚后校验完整原字段哈希。OSS 新对象不立即删除，以免回滚后再次切换时丢失。

## 10. 完成标准

第一阶段完成标准：

- 28 张唯一图片均有 SVG 和 PNG 产物。
- 28 张均通过自动检查和人工检查。
- 联系表和审核报告可供用户逐张查看。
- manifest 记录 28 个 asset、33 个 occurrence、32 个题目，并带完整字段哈希和产物哈希。
- 用户批准的 manifest digest 与全部 28 个 PNG digest 已记录，且批准后文件未变化。
- 数据库仍保持原 URL，不发生线上或本地题库迁移。

第二阶段完成标准：

- 28 张 PNG 全部成功上传 OSS 并可访问。
- 33 次图片 URL 引用全部精确替换。
- 32 条目标题目的 33 个旧图片节点全部清零，33 个新图片节点存在，非目标内容字节级不变。
- 题目接口和答题页面显示正常。
- 备份已在临时 schema 中恢复验证，普通回滚与并发保护规则完整可用。
- SVG、批准 manifest、审核报告和迁移工具已进入版本化长期存储；本地备份按约定保留至少 90 天。
